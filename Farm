-- LOAD RAYFIELD
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
	Name = "Trung Kien Hub",
	LoadingTitle = "Loading...",
	LoadingSubtitle = "by Kien",
	ConfigurationSaving = {Enabled = false}
})

local Tab = Window:CreateTab("Farm", 4483362458)

-- SERVICES
local Players = game:GetService("Players")
local VirtualUser = game:GetService("VirtualUser")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local player = Players.LocalPlayer

-- SETTINGS
local FARM = false
local HEIGHT = 6
local DELAY = 0.03

-- 📜 CHECK QUEST
local function hasQuest()
	local gui = player.PlayerGui:FindFirstChild("Main")
	if gui and gui:FindFirstChild("Quest") then
		return gui.Quest.Visible
	end
	return false
end

-- 📜 QUEST NPC
local function getQuestNPC()
	for _,v in pairs(workspace:GetDescendants()) do
		if v:IsA("Model")
		and v:FindFirstChild("Head")
		and string.find(v.Name:lower(), "quest") then
			return v
		end
	end
end

-- 📜 AUTO QUEST
local function autoQuest()
	if hasQuest() then return end
	
	local npc = getQuestNPC()
	if not npc then return end
	
	local char = player.Character
	if not char or not char:FindFirstChild("HumanoidRootPart") then return end
	
	local root = char.HumanoidRootPart
	
	root.CFrame = npc.Head.CFrame * CFrame.new(0,0,3)
	task.wait(0.5)
	
	pcall(function()
		ReplicatedStorage.Remotes.CommF_:InvokeServer("StartQuest", npc.Name, 1)
	end)
end

-- 👹 CHỈ TÌM THIEF
local function getThief(char)
	local nearest, dist = nil, math.huge
	
	for _,v in pairs(workspace:GetDescendants()) do
		if v:IsA("Model")
		and v.Name == "Thief"
		and v:FindFirstChild("Humanoid")
		and v:FindFirstChild("HumanoidRootPart")
		and v.Humanoid.Health > 0 then
			
			local d = (char.HumanoidRootPart.Position - v.HumanoidRootPart.Position).Magnitude
			
			if d < dist then
				dist = d
				nearest = v
			end
		end
	end
	
	return nearest
end

-- ATTACK
local function attack()
	VirtualUser:Button1Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
	task.wait()
	VirtualUser:Button1Up(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
end

-- QUEST LOOP
task.spawn(function()
	while true do
		task.wait(2)
		if FARM then
			autoQuest()
		end
	end
end)

-- FARM LOOP
task.spawn(function()
	local bv
	
	while true do
		task.wait(DELAY)
		
		if not FARM then
			if bv then bv:Destroy() bv=nil end
			continue
		end
		
		local char = player.Character
		if not char or not char:FindFirstChild("HumanoidRootPart") then continue end
		
		-- ❗ chưa có quest thì đợi
		if not hasQuest() then continue end
		
		local root = char.HumanoidRootPart
		local npc = getThief(char)
		
		if npc then
			local tRoot = npc.HumanoidRootPart
			
			if not bv then
				bv = Instance.new("BodyVelocity")
				bv.MaxForce = Vector3.new(1e9,1e9,1e9)
				bv.Parent = root
			end
			
			local pos = tRoot.Position + Vector3.new(0, HEIGHT, 0)
			bv.Velocity = (pos - root.Position) * 6
			
			root.CFrame = CFrame.new(root.Position, tRoot.Position)
			
			tRoot.Size = Vector3.new(10,10,10)
			tRoot.CanCollide = false
			
			attack()
		end
	end
end)

-- TOGGLE
Tab:CreateToggle({
	Name = "Auto Farm Thief",
	CurrentValue = false,
	Callback = function(Value)
		FARM = Value
	end
})
